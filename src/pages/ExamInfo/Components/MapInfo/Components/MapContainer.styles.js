import styled from 'styled-components';

export const PlaceMap = styled.div`
  ${({ theme }) => theme.variables.wh('400px', '400px')}
  margin-right: 20px;
  box-shadow: rgba(99, 99, 99, 0.2) 0px 2px 8px 0px;
`;
